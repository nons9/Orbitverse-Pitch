name: Build Pitch Deck

on:
  push:
    branches:
      - main
    paths:
      - 'pitch_deck/**'
      - '.github/workflows/deck.yml'

concurrency:
  group: build-pitch-deck
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Prepare fonts (optional)
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto

      - name: Install Marp CLI & Puppeteer globally
        run: npm install -g @marp-team/marp-cli puppeteer

      - name: Verify Marp CLI
        run: marp --version

      - name: Render pitch deck
        run: |
          mkdir -p docs
          marp pitch_deck/slides.md \
            --theme moon \
            --html \
            --highlight-theme solarized-light \
            -o docs/index.html

      - name: Commit & push updated deck
        run: |
          if git diff --quiet docs; then
            echo "No changes to commit"
          else
            git config user.name "OrbitBot"
            git config user.email "actions@github.com"
            git add docs
            git commit -m "chore: auto-build pitch deck"
            git push
          fi
